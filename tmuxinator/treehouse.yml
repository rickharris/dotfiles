# ~/.tmuxinator/treehouse.yml
# you can make as many tabs as you wish...

project_name: treehouse
project_root: ~/Code/treehouse/web
tabs:
  - editor: vi
  - utility:
      layout: tiled
      panes:
        -
        - exit
        - powder applog
        - cd ../challenges && bundle exec ./worker
