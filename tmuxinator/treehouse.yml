name: Treehouse
root: ~/Code/treehouse/treehouse
pre_window: rvm use `cat .ruby-version` && rvm gemset use `cat .ruby-gemset`
windows:
  - editor: vi
  - shell:
  - console: rails c
  - server: rails s
